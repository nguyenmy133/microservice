spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:broker:9092}
    consumer:
     group-id: ${KAFKA_GROUP_ID:defalut-group}
  mail:
    host: smtp.gmail.com
    port: 587
    username: mynguyen13324@gmail.com
    password: liug uzha lhit pmgb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        transport:
          protocol: true

